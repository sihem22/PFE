{% extends ":front:Front.html.twig" %}
{% block title %}Compétences{% endblock  %}
{% block body %}
    
    <div class="container">
        <hr>
        {% include ':front:profil.html.twig' %}
            <div class="col-md-6">

   
     <form method="post">
        {% for categorie in categories %}
            {% if true %}
                <div class="row">
                    <div class="col-md-8">
                        <div class="panel-group accordion-custom accordion-teal" id="accordion">
                            <div class="panel panel-default">
                                <div class="panel-heading">
                                    <h4 class="panel-title">
                                        <a class="accordion-toggle collapsed" href="#collapse-{{ categorie.id }}" data-toggle="collapse" data-parent="#accordion">
                                            <i class="icon-arrow"></i>
                                            {{ categorie.nom }}

                                        </a></h4>
                                </div></div>

                            <div class="panel-collapse collapse" id="collapse-{{ categorie.id }}" style="height: 0px;">
                                <div class="panel-body">
                                    {% for service in categorie.service %}
                                        <p>


                                            <input name="avc[]" value="{{ service.id }}" type="checkbox" >{{service.nom}}

                                        </p>
                                    {%endfor%}
                                </div>
                            </div>
                        </div>  </div>    </div>{%endif%} 
                            {%endfor%} 

                                 <div class="row">
                                        <div class="col-md-3">
                                            <button class="btn btn-bricky btn-lg" type="submit">
                                                Enregistrer
                                            </button></div></div> </form></div></div></div>
                {%endblock%}